// Arrays:-> List of Elements of same data type stored in contiguous memory locations.
// Contiguous memory locations means that the elements of the array are stored in adjacent memory locations, which allows for efficient access and manipulation of the array elements.
// Arrays are fixed in size, meaning that once an array is created, its size cannot be changed.
// Arrays indexing starts from 0, which means that the first element of the array is accessed using index 0, the second element using index 1, and so on.
